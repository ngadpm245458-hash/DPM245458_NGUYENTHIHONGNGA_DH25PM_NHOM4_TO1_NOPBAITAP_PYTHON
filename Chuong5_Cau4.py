"""
1. Hàm kiểm tra (Check Methods)
Các hàm này thường trả về True hoặc False.

s.isalnum(): Kiểm tra xem chuỗi có chỉ chứa các ký tự chữ cái (a-z, A-Z) và số (0-9) hay không.

s.isalpha(): Kiểm tra xem chuỗi có chỉ chứa các ký tự chữ cái hay không.

s.isdigit(): Kiểm tra xem chuỗi có chỉ chứa các ký tự số hay không.

s.islower(): Kiểm tra xem tất cả các ký tự chữ cái trong chuỗi có phải là chữ thường hay không.

s.isupper(): Kiểm tra xem tất cả các ký tự chữ cái trong chuỗi có phải là chữ hoa hay không.

s.startswith(prefix): Kiểm tra chuỗi có bắt đầu bằng prefix không.

s.endswith(suffix): Kiểm tra chuỗi có kết thúc bằng suffix không.

2. Hàm thay đổi/Chuyển đổi (Modification/Conversion Methods)
Các hàm này trả về một chuỗi mới đã được biến đổi (chuỗi gốc không bị thay đổi).

s.upper(): Chuyển tất cả các ký tự chữ cái trong chuỗi thành chữ hoa.

s.lower(): Chuyển tất cả các ký tự chữ cái trong chuỗi thành chữ thường.

s.capitalize(): Chuyển ký tự đầu tiên của chuỗi thành chữ hoa, các ký tự còn lại thành chữ thường.

s.strip(): Loại bỏ khoảng trắng (hoặc ký tự chỉ định) ở đầu và cuối chuỗi.

s.replace(old, new): Thay thế tất cả các lần xuất hiện của chuỗi old bằng chuỗi new.

3. Hàm tìm kiếm và định dạng (Searching and Formatting Methods)
s.find(substring): Trả về chỉ số (index) của lần xuất hiện đầu tiên của chuỗi con. Trả về -1 nếu không tìm thấy.

s.count(substring): Đếm số lần chuỗi con xuất hiện trong chuỗi.

s.split(delimiter): Tách chuỗi thành một danh sách (list) các chuỗi con dựa trên ký tự phân tách (delimiter).

delimiter.join(list): Ghép các phần tử của một danh sách thành một chuỗi duy nhất, sử dụng delimiter để phân tách.
"""
